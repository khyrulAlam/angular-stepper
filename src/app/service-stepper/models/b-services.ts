export interface IService {
    startTime: string;
    serviceName: string;
    duration: string;
    staffName: string;
    durationValue: number;
}
export interface IServiceList {
    name: string;
    nEu: string;
    duration: string;
    durationValue: number;
}